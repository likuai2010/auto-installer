# Run with `dart run ffigen --config ffigen.yaml`.
name: NativeCoreBindings
description: |
  Bindings for `src/native_core.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/native_core_bindings_generated.dart'
headers:
  entry-points:
    - 'src/native_core.h'
  include-directives:
    - 'src/native_core.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
